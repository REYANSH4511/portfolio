'use client'

export default function HeroScene() {
  // HeroScene is not used directly — Hero.tsx imports
  // ParticleField and FloatingGeometry independently via next/dynamic.
  // This file is kept as a no-op to avoid import errors if referenced elsewhere.
  return null
}
